import React from 'react'

const OTP = ({ onNext }) => {
    return (
        <div>
            this is otp
            <button onClick={onNext}>Next</button>

        </div>
    )
}

export default OTP
